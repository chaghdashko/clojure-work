(defn example []
  ;; declares an integer variable
  (def x 1)

  ;; declares a float variable
  (def y 1.25)

  ;; declares a string variable
  (def str1 "Hello")

  (println x)
  (println y)
  (println str1))

(example)
